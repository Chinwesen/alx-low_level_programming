#include "main.h"

/**
 * add - prints the sum between two values
 * @a: value 1
 * @b: value 2
 * Return: success 1
 * on error, -1 is returned
 */
int add(int a, int b)
{
	int r;

	r = (a + b);
	return (r);
}
